import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pile.un.ms.mobile',
  appName: 'PileMobile',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
